To build and run:

1. Save the files as shown
2. Create build directory:
```bash
mkdir build && cd build
```
3. Generate build files:
```bash
cmake ..
```
4. Build:
```bash
cmake --build .
```
5. Run tests:
```bash
.\calculator_test
```
